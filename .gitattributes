# Large mesh assets are stored in Git LFS, not regular Git objects.
# Root-level so the patterns cover every path (apps/dev-host/assets moved here
# in phase C; the copies under urdf_viewer/ and _slicer_branch/ remain for
# their own subtrees).
*.obj filter=lfs diff=lfs merge=lfs -text
*.glb filter=lfs diff=lfs merge=lfs -text
*.gltf filter=lfs diff=lfs merge=lfs -text

# Generated by tools/gen_dom_contract.mjs, which writes LF. Without this, git
# checks it out with CRLF on Windows and gate 8 reports the file as stale on
# every fresh checkout — a gate that cries wolf gets ignored.
contract-dom.json text eol=lf
