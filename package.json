{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:dev": "node build.mjs --dev",
    "lint": "eslint hmi viewer apps/dev-host/src/avisualizer/web/static",
    "test": "node --test \"tests/js/**/*.test.mjs\"",
    "gate": "bash gate.sh"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "eslint": "^9.13.0",
    "globals": "^15.11.0",
    "jsdom": "^30.0.1"
  }
}
