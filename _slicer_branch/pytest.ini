[pytest]
# Silence a third-party deprecation we don't control: starlette's TestClient
# warns about its own internal httpx usage. Our code is unaffected; revisit if
# we bump starlette/httpx.
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated.*
